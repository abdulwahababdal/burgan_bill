name: burgan_bill
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_native_splash: ^2.1.6
  cupertino_icons: ^1.0.2
  go_router: ^14.3.0
  provider: ^6.1.2
  dio: ^5.7.0
  image_picker: ^1.1.2
  build_runner: ^2.1.8
  json_serializable: ^6.1.5
  shared_preferences: ^2.3.2
  jwt_decode: ^0.3.1
  video_player: ^2.9.2
  url_launcher: ^6.3.1
  flutter_animate: ^4.5.0
  flutter_launcher_icons: ^0.14.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.14.1

flutter:
  uses-material-design: true
  assets:
    - assets/images/

flutter_icons:
  image_path: "assets/images/buranbill.png"
  android: true
  ios: true
